---
phase: 06-support-all-pyarrow-types
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [EXT-FLOAT16, EXT-DEC128, EXT-DEC256, EXT-DEC32, EXT-DEC64, EXT-DATE64, EXT-TIME32, EXT-TIME64, EXT-INTERVAL, EXT-BINARY, EXT-FSBINARY, EXT-UTF8VIEW, EXT-BINVIEW, EXT-FSLIST, EXT-MAP, EXT-REE, EXT-UNION]

must_haves:
  truths:
    - "All 17 EXT-* requirement IDs appear in REQUIREMENTS.md with definitions"
    - "All 17 EXT-* requirement IDs appear in the Traceability table with Phase 6 assignment"
    - "Coverage count is updated to include the 17 new requirements"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 6 Extended Types requirement definitions and traceability rows"
      contains: "EXT-FLOAT16"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md Phase 6"
      via: "Requirement IDs matching ROADMAP Requirements line"
      pattern: "EXT-FLOAT16.*Phase 6"
---

<objective>
Add all 17 Phase 6 requirement IDs (EXT-FLOAT16 through EXT-UNION) to REQUIREMENTS.md. Currently these IDs exist only in PLAN frontmatter and ROADMAP.md but are missing from the formal requirements document, leaving the traceability table incomplete.

Purpose: Close the REQUIREMENTS.md documentation gap identified in VERIFICATION.md. All requirement IDs referenced by plans must exist in REQUIREMENTS.md for traceability.
Output: Updated REQUIREMENTS.md with Phase 6 requirement definitions and traceability rows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-support-all-pyarrow-types/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 6 requirement definitions and traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file -- understand existing structure, requirement format, traceability table format)
    - .planning/ROADMAP.md (Phase 6 Requirements line for exact IDs)
    - .planning/phases/06-support-all-pyarrow-types/06-VERIFICATION.md (Requirements Coverage table for descriptions and statuses)
  </read_first>
  <action>
Make three changes to .planning/REQUIREMENTS.md:

**Change 1: Add Extended Types (Phase 6) section to v1 Requirements**

After the existing "### Complex Types" section (ends around line 68) and before "### Input Types" (starts around line 70), add a new section:

```markdown
### Extended Types (Phase 6)

- [x] **EXT-FLOAT16**: Float16 -> `float` (upcast to f32 for Python compatibility)
- [x] **EXT-DEC128**: Decimal128 -> `decimal.Decimal` (precision preserved via string representation)
- [x] **EXT-DEC256**: Decimal256 -> `decimal.Decimal` (precision preserved via string representation)
- [x] **EXT-DEC32**: Decimal32 -> `decimal.Decimal` (precision preserved via string representation)
- [x] **EXT-DEC64**: Decimal64 -> `decimal.Decimal` (precision preserved via string representation)
- [x] **EXT-DATE64**: Date64 -> `datetime.datetime` (millisecond-epoch to datetime)
- [x] **EXT-TIME32**: Time32 (second/millisecond) -> `datetime.time`
- [x] **EXT-TIME64**: Time64 (microsecond/nanosecond) -> `datetime.time` (ns truncated to us)
- [x] **EXT-INTERVAL**: Interval (YearMonth/DayTime/MonthDayNano) -> `tuple[int, int, int]` (months, days, nanos)
- [x] **EXT-BINARY**: Binary/LargeBinary -> `bytes`
- [x] **EXT-FSBINARY**: FixedSizeBinary -> `bytes`
- [x] **EXT-UTF8VIEW**: Utf8View -> `str` (identical to Utf8 path)
- [x] **EXT-BINVIEW**: BinaryView -> `bytes` (identical to Binary path)
- [x] **EXT-FSLIST**: FixedSizeList -> `list` (recursive element type handling)
- [x] **EXT-MAP**: Map -> `list[tuple[K, V]]` (key-value pair extraction)
- [x] **EXT-REE**: RunEndEncoded -> transparently pre-unpacked to underlying value type
- [x] **EXT-UNION**: Union (sparse + dense) -> active variant's value per row
```

**Change 2: Add Phase 6 rows to the Traceability table**

After the last row in the Traceability table (API-05 | Phase 5 | Complete), add 17 new rows:

```markdown
| EXT-FLOAT16 | Phase 6 | Complete |
| EXT-DEC128 | Phase 6 | Complete |
| EXT-DEC256 | Phase 6 | Complete |
| EXT-DEC32 | Phase 6 | Complete |
| EXT-DEC64 | Phase 6 | Complete |
| EXT-DATE64 | Phase 6 | Complete |
| EXT-TIME32 | Phase 6 | Complete |
| EXT-TIME64 | Phase 6 | Complete |
| EXT-INTERVAL | Phase 6 | Complete |
| EXT-BINARY | Phase 6 | Complete |
| EXT-FSBINARY | Phase 6 | Complete |
| EXT-UTF8VIEW | Phase 6 | Complete |
| EXT-BINVIEW | Phase 6 | Complete |
| EXT-FSLIST | Phase 6 | Complete |
| EXT-MAP | Phase 6 | Complete |
| EXT-REE | Phase 6 | Complete |
| EXT-UNION | Phase 6 | Complete |
```

**Change 3: Update the Coverage counts**

Change the coverage section from:
```
- v1 requirements: 42 total
- Mapped to phases: 42
- Unmapped: 0
```
to:
```
- v1 requirements: 59 total
- Mapped to phases: 59
- Unmapped: 0
```

(42 existing + 17 new = 59)

**Change 4: Move v2 Extended Types to avoid confusion**

The existing v2 section has "### Extended Types" with EXT-01, EXT-02, EXT-03. Rename that section header to "### Extended Types (Future)" to distinguish from the new Phase 6 section. These v2 items remain as-is (they are separate future features like FixedSizeList resolution heuristic, Utf8View/BinaryView, and Binary/LargeBinary which have been superseded by the Phase 6 EXT-* IDs but represent broader feature scopes).

**Change 5: Update the "Last updated" date**

Change the last line from:
```
*Last updated: 2026-03-21 after roadmap revision (Phase 2 split into Spike & Benchmark + Core Conversion)*
```
to:
```
*Last updated: 2026-03-22 after Phase 6 gap closure (added 17 EXT-* requirement definitions)*
```
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/arrowdantic && grep -c "EXT-" .planning/REQUIREMENTS.md && echo "---" && grep "EXT-FLOAT16" .planning/REQUIREMENTS.md && echo "---" && grep "EXT-UNION" .planning/REQUIREMENTS.md && echo "---" && grep "59 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "EXT-FLOAT16" .planning/REQUIREMENTS.md returns 2 (one in definition, one in traceability table)
    - grep -c "EXT-UNION" .planning/REQUIREMENTS.md returns 2 (one in definition, one in traceability table)
    - grep -c "Phase 6" .planning/REQUIREMENTS.md returns at least 17 (one per traceability row)
    - grep "59 total" .planning/REQUIREMENTS.md returns a match
    - All 17 EXT-* IDs appear in both the requirements section and traceability table
    - v2 Extended Types section renamed to "Extended Types (Future)"
    - Last updated date shows 2026-03-22
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains all 17 Phase 6 requirement definitions with [x] completion markers, all 17 traceability rows mapped to Phase 6 with Complete status, and updated coverage count of 59 total v1 requirements.</done>
</task>

</tasks>

<verification>
Verify all 17 EXT-* IDs are present in both sections:
```bash
for id in EXT-FLOAT16 EXT-DEC128 EXT-DEC256 EXT-DEC32 EXT-DEC64 EXT-DATE64 EXT-TIME32 EXT-TIME64 EXT-INTERVAL EXT-BINARY EXT-FSBINARY EXT-UTF8VIEW EXT-BINVIEW EXT-FSLIST EXT-MAP EXT-REE EXT-UNION; do
  count=$(grep -c "$id" .planning/REQUIREMENTS.md)
  echo "$id: $count occurrences"
done
```
Each ID should appear exactly 2 times (definition + traceability).
</verification>

<success_criteria>
- All 17 EXT-* requirement IDs exist in REQUIREMENTS.md definitions section
- All 17 EXT-* requirement IDs exist in REQUIREMENTS.md traceability table with Phase 6 and Complete status
- Coverage count updated to 59 (42 + 17)
- No formatting or structural issues in the markdown
</success_criteria>

<output>
After completion, create `.planning/phases/06-support-all-pyarrow-types/06-04-SUMMARY.md`
</output>
