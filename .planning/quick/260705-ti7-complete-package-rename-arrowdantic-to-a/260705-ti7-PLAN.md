---
quick_id: 260705-ti7
title: Complete package rename arrowdantic → arrowmodel (remaining references and filenames)
date: 2026-07-05
status: ready
---

## Objective

The bulk of the `arrowdantic` → `arrowmodel` rename is already committed (`src/arrowmodel/`
directory, `pyproject.toml` name/module-name, `rust/Cargo.toml` crate names, README, imports).
This task closes out the stragglers: the last textual `arrowdantic` references and two filenames
that still carry the old name.

## Scope (verified by grep, excluding build artifacts / .git / worktrees)

| File | Occurrences | Kind | Action |
|------|-------------|------|--------|
| `src/arrowmodel/_core.pyi` | 1 | stub module docstring `arrowdantic._core` | text → `arrowmodel._core` |
| `benchmarks/bench_convert.py` | 4 | code comments `# arrowdantic` | text → `arrowmodel` |
| `tests/test_arrowdantic.py` | filename only (0 content) | filename | `git mv` → `tests/test_arrowmodel.py` |
| `_notes/arrowdantic-design.md` | 6 internal + filename | design doc | replace refs + `git mv` → `_notes/arrowmodel-design.md` |
| `.planning/PROJECT.md` | 3 (title + `arrowdantic._core` refs) | planning doc | text → `arrowmodel` |

## Out of scope (intentionally untouched)

- The other uncommitted working-tree changes (`README.md` rewrite, `docs/`, `.doc-writer/`,
  `MAINTAINER.md`, `justfile`, `pyproject.toml` docs group, `uv.lock`, `__init__.py` docstring
  fix) are an **unrelated mkdocs→sphinx docs migration**, not part of the rename. They are left
  uncommitted and untouched so the rename commit stays atomic.
- Build artifacts under `rust/target/` (old wheel, fingerprints) — regenerated on next build.

## Tasks

1. Text substitution in `_core.pyi`, `bench_convert.py`, `PROJECT.md` (`arrowdantic` → `arrowmodel`).
2. `git mv` the two old-named files and update the design doc's internal references.
3. Verify: `grep -rI arrowdantic` returns nothing outside build artifacts; `uv run pytest` passes.

## Verify

- `git ls-files | grep arrowdantic` → empty
- Remaining `arrowdantic` only in `rust/target/` build artifacts and `.planning/` history/logs
- Test suite still green after the test-file rename
